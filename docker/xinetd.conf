# Simple xinetd configuration for the IRIX install server container.
#
# Debian Bookworm's xinetd default can fail in this minimal container when it
# tries to initialize syslog logging. Use a normal container-local log file.

defaults
{
    log_type = FILE /var/log/xinetd.log
}

includedir /etc/xinetd.d
